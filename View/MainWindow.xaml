<Window x:Class="LevelEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LevelEditor.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <ScrollViewer Background="#70999999">
                <DockPanel>
                    <Button DockPanel.Dock="Top" Height="50" Width="50" VerticalAlignment="Top" Margin="0,20,0,0"></Button>
                    <Button DockPanel.Dock="Top" Height="50" Width="50" VerticalAlignment="Top" Margin="0,20,0,0"></Button>
                    <Button DockPanel.Dock="Top" Height="50" Width="50" VerticalAlignment="Top" Margin="0,20,0,0"></Button>
                </DockPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
